require 'easy_ruby_distributor'

EasyRubyDistributor.define { |options|
  options.use_gemspec_for_details # or "filename"
  options.output_dir = 'releases'
  #options.version = File.read('VERSION')
  options.use_batch_files_to_run_bin = "lib\run_generic_rb_file.bat"
  options.run_with=
 #?? options.bundle_ruby='
}